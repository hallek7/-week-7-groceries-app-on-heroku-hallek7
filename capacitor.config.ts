import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'week-3-adding-remove-items',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
